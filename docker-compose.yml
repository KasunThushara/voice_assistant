version: "3.9"

services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    depends_on:
      - vosk
      - ollama

  vosk:
    image: alphacep/kaldi-vosk-server
    ports:
      - "2700:2700"

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ./ollama-models:/root/.ollama
